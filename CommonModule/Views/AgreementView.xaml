<UserControl x:Class="CommonModule.Views.AgreementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             >
    <Grid>
            <StackPanel Margin="3">
                <TextBlock Text="{Binding FullAgreeName, StringFormat='№ {0}'}" 
                                          TextWrapping="Wrap" TextAlignment="Left"/>
                <TextBlock Text="{Binding DateOfDocument, StringFormat='от {0:dd.MM.yyyy}'}" 
                                          TextWrapping="Wrap" HorizontalAlignment="Left" TextAlignment="Left"/>
                <TextBlock Text="{Binding Contents}"
                                          TextWrapping="Wrap" TextAlignment="Left" 
                                          FontSize="10" FontStyle="Italic"
                                          Margin="0,3,0,0"/>
            </StackPanel>
    </Grid>
</UserControl>
